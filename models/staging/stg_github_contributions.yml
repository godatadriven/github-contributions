version: 2

models:
- name: stg_pull_requests
  columns:
  - name: user_login
    tests:
    - accepted_values:
        values: "{{ var('authors') }}"

- name: stg_repositories
  tests:
  - dbt_utils.expression_is_true:
      expression: "NOT private"
