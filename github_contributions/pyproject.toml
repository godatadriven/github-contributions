[build-system]
requires = [
    "setuptools >= 82.0.1",
    "wheel >= 0.41.0",
]
build-backend = 'setuptools.build_meta'

[tool.black]
line-length = 79
target-version = ['py311']

[tool.isort]
profile = "black"
src_paths = ["src", "tests"]
