[build-system]
requires = [
    "setuptools >= 68.0.0",
    "wheel >= 0.47.0",
]
build-backend = 'setuptools.build_meta'

[tool.black]
line-length = 79
target-version = ['py311']

[tool.isort]
profile = "black"
src_paths = ["src", "tests"]
